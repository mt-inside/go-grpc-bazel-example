apiVersion: 1

providers:
  - name: dashboards
    orgId: 1
    folder: '' # grafana UI folder
    type: file
    editable: true
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
